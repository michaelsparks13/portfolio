import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Michael Sparks — Climate & Weather Tech Engineer",
  description:
    "Building AI-powered tools to understand Earth's weather systems, monitor environmental change, and make climate data actionable. Portfolio featuring 11 projects in weather AI, satellite remote sensing, and climate data engineering.",
  openGraph: {
    title: "Michael Sparks — Climate & Weather Tech Engineer",
    description:
      "Building AI-powered tools to understand Earth's weather systems.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
